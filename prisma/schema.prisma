generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Lead {
  id    String  @id @default(uuid())
  email String  @unique
  name  String?

  @@map("leaders")
}
